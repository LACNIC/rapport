ta.cer
	ca.cer
		route.roa
		ca.mft
		ca.crl
	ta.mft
	ta.crl

[node: ta.mft]
obj.content.encapContentInfo.eContent.manifestNumber = 1

[node: route.roa]
obj.content.encapContentInfo.eContent.ipAddrBlocks = [ 1.1.0.0/24 ]
obj.content.encapContentInfo.eContent.asId = 64520

# The manifest references B's hash. The snapshot and notification are
# generated correctly for B; run.sh then injects A as a duplicate <publish>
# entry for the same URI to reproduce the issue.
[notification: https://localhost:8443/$TEST/notification.xml]
session = cafe
serial = 1
